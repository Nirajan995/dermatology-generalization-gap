# CPU-only analysis (probes, kNN purity, adaptation, figures, null calibration)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# Baseline training + feature extraction (needs a GPU); see requirements-full.txt
